apiVersion: v1
kind: Service
metadata:
  name: devops-dashboard-service
  namespace: default
  labels:
    app: devops-dashboard
spec:
  selector:
    app: devops-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: NodePort
